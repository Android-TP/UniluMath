<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.daniel.unilumath">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/hide">
        <activity
            android:name=".Etudiant"
            android:label="@string/title_activity_etudiant"
            android:theme="@style/hide.NoActionBar"></activity>

        <activity
            android:name=".Preinscription"
            android:label="@string/title_activity_preinscription"
            android:theme="@style/hide.NoActionBar" />
        <activity android:name=".ActionPrincipale" />
        <activity android:name=".connexion" />
        <activity android:name=".inscription" />
        <activity android:name=".Accueil_activity" />
        <activity android:name=".MainActivity2" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>